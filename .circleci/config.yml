version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.5.0-browsers

    working_directory: ~/my-book-test

    steps:
      - checkout

      - run: npm install
      - run: npm run build
      - run: npm test:ci
      - run: npm test:e2e
      - run: npm test:performance
